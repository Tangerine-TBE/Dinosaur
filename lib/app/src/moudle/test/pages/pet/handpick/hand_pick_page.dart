import 'package:flutter/material.dart';

class HandPickPage extends StatelessWidget {
  const HandPickPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('3'),);
  }
}
